'use strict';

/**
 * responsibilities-ministry service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::responsibilities-ministry.responsibilities-ministry');
