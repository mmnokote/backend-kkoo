'use strict';

/**
 * responsibilities-ministry router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::responsibilities-ministry.responsibilities-ministry');
